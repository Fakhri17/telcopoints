{% set data = {
  class: '',
  title: '',
  subtitle: ''
}|merge(data ?? {}) %}

<div class="p-5 text-center bg-colors h-50">
  <div class="container h-100">
    <div class="row align-items-center h-100">
      <div class="col-8 mx-auto">
        <div class="jumbotron">
         <h2 class="text-white fw-bold mb-3">{{ data.title }}</h2>
         <nav aria-label="breadcrumb">
          <ol class="breadcrumb justify-content-center">
            <li class="breadcrumb-item"><a href="{{ url('/')}}" class="text-white">Home</a></li>
            <li class="breadcrumb-item text-white text-decoration-underline" aria-current="page">{{ data.subtitle }}</li>
          </ol>
        </nav>
        </div>
      </div>
    </div>
  </div>
</div>